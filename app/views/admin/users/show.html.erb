<% content_for :title, @user.full_name %>
<h1 class="text-xl font-bold"><%= @user.full_name %></h1>
<p class="text-sm text-slate-600"><%= @user.email_address %> — <%= @user.role %></p>
<%= link_to "Edit", edit_admin_user_path(@user), class: "mt-4 inline-block text-indigo-600 underline" %>
